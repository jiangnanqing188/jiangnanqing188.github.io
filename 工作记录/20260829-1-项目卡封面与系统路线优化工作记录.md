# 20260829-1 项目卡封面与系统路线优化工作记录

## 日期与目标

- 日期：2026-08-29。
- 目标：修正项目档案卡片左侧封面被正文高度拉成长图的问题，并将修正比例后产生的空白改造成有实际信息价值的项目系统路线。

## 开始时分支和基线

- 仓库：`/mnt/data/jiangnanqing.github.io`。
- 分支：`master`。
- 开始基线：`87783443566b258a468a2fbe1ff56633777744ae`。
- 开始时工作区：干净。

## 完成内容

- 将四张项目封面统一按源图 `16:9` 比例展示，不再随右侧项目正文等高裁切。
- 将项目卡左栏整理为“封面 + SYSTEM ROUTE”项目轨道，填补原有无信息空白。
- 为睿抗、智慧药房、智慧农业 YOLOv5 和 ISCC 分别增加四个真实项目链路节点。
- 桌面端使用纵向路线连接四个节点；移动端改为两列紧凑路线，避免无意义拉长页面。
- 保持右侧项目摘要、职责、协作边界、结果、证据和文章记录不变。

## 涉及文件

- `source/_data/projects.yml`：四个项目的系统路线数据。
- `scripts/lab-site.js`：项目左栏和系统路线的渲染结构。
- `source/css/custom.css`：封面比例、桌面纵向路线和移动端两列布局。
- `工作记录/20260829-1-项目卡封面与系统路线优化工作记录.md`：本次工作记录。

## 关键决策

- 不用虚构代码、技能百分比、重复奖项或装饰图案填充左栏。
- 系统路线只概括已有项目事实，不将其伪装为比赛期间留下的架构证据。
- 保留现有深色工程档案视觉，以蓝色路线和橙色节点表达系统推进顺序。

## 验证

- `node --check scripts/lab-site.js`：通过。
- `git diff --check`：通过。
- `npm run build`：通过。
- 生成页面检查：四个项目均输出 `SYSTEM ROUTE`，项目专属节点进入最终 HTML。
- 桌面端 `1989×3000` 截图：封面比例正常，左栏路线完整填充，右侧内容未受影响。
- 移动端 `390×4000` 截图：路线为两列，无横向溢出，长节点可正常换行。
- 线上检查：项目页输出 4 组 `SYSTEM ROUTE`，线上 CSS 已加载 `.lab-projects__rail` 新样式。

## 当前状态、未完成事项与风险

- 当前状态：本地实现、视觉检查、源码推送和 GitHub Pages 部署均已完成。
- 未完成事项：文章重构、项目原始证据盘点和睿抗证书正式文字不属于本次任务。
- 风险：系统路线属于项目概览，不应在后续文章中表述为比赛期间形成的架构图或原始证据。

## Git 提交与回退

- 源码提交：`2e682b2 feat: 完善项目卡系统路线`。
- 部署提交：`aa47cb0 Site updated: 2026-08-29 03:03:18`。
- 回退方式：使用 `git revert <本次提交>` 同时撤销路线数据、渲染结构和样式，不直接修改既有历史。
