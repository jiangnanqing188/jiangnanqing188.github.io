# 2026-08-23 工作记录

## 日期与目标

- 日期：2026-08-23
- 目标：根据参赛负责人的一手纠正，修复睿抗赛94分复盘中未确认的距离、未闭环的点9叙述以及8月18日至20日的错位时间线；修复首页文章标题过长时与标签、时间重叠的问题；为睿抗复盘更换专属封面。

## 开始基线

- 仓库：`/mnt/data/jiangnanqing.github.io`
- 分支：`master`
- 开始 HEAD：`eceba3c17f9ef79b61606dbc8530d4055bba24a6`
- 开始工作区：干净
- 首页样式优化开始 HEAD：`c58793f702a31a99245d076a46447b5f92733b67`，工作区干净。

## 完成内容

- 删除“点5离点4交接位置大约一米”的确定说法，仅保留已有记录支持的“点5绕行和转动、耗时将近79秒”。
- 补全点9故障的证据边界：8月6日形成了纯旋转前停车、清理代价地图、等待1秒再发目标和按实车尺寸修正 footprint 的候选，但当时没有重复实车验收。
- 补全后续现场结果：8月14日路线已调整为43点并切换 Noetic，工程师将 odom 的 IMU 话题改为 `/wit/imu`、`imu0_relative` 改为 `false`，之后负责人现场看到43点路线完整跑通。
- 明确无法把旧点9问题归因于某一项修改，因为路线、ROS 环境和 odom 配置同期发生了变化。
- 重排最后三天的时间线：
  - 8月18日收尾时保留含黄线和无黄线两份主代码。
  - 8月19日负责人到现场测试，黄线效果不好，最终选择纯点位版本；同日发现枪不射击、云台无动作，现场工程师最后发现没有插电。
  - 8月20日正式比赛使用纯点位版本，整个正式比赛没有再出问题，最终获得94分。
- 同步修改文章标题、摘要、开头、日期小节和结果回扣，使全文不再沿用错误时间线。
- 将首页文章卡片信息区改为纵向弹性布局，取消标签和时间的绝对定位；元信息通过自动外边距稳定排在标题、摘要之后，桌面端和移动端统一使用 `200px` 最小信息区高度。
- 缩小 769px 至 1200px 宽度下摘要的下边距，为两行标题和底部元信息留出稳定空间。
- 生成睿抗比赛专属横版封面，画面包含室内比赛赛道、点位、黄线、侦察车、调试电脑和未接电源线；转换为 `1200×675 WebP` 后更新文章封面引用。

## 涉及文件

- `source/_posts/睿抗赛94分复盘.md`：修正比赛复盘的事实和时间线，并更新封面引用。
- `source/img/covers/ruikang-competition.webp`：睿抗比赛专属封面。
- `source/css/custom.css`：修复首页长标题与标签、时间重叠。
- `工作记录/20260823-工作记录.md`：记录修正依据、验证和回退方式。

## 关键决策与证据边界

- 点4到点5的精确距离当前未知，不再使用“大约一米”。
- 8月6日的纯旋转前清图与 footprint 修正只是当时的本地候选，不写成已实车验收的修复。
- 8月14日的43点路线跑通有负责人现场观察支持，但缺少 rosbag、终端日志和重复次数，不写成单变量或可回放验收。
- 8月19日“没有插电”和8月20日“正式比赛没有问题”来自负责人本次一手纠正；原工程记录在8月19日当时只记到“云台无实际响应，供电待查”。
- 长标题问题与鼠标悬停无关；根因是主题在固定高度信息区内绝对定位元信息，而自定义 `LOG` 状态行和两行标题占用了同一垂直空间。修复保留两行标题，不通过改短标题或隐藏标签规避问题。
- 新封面使用独立文件，不覆盖仍被其他文章使用的 `ai-robot-tuning.webp`；画面不加入标题、比分、Logo 或水印，避免响应式裁切和生成文字错误。

## 验证命令与结果

- `npm run build`：通过，Hexo 生成48个文件，包含 `2026/08/21/睿抗赛94分复盘/index.html`。
- `git diff --check`：通过，无空白错误。
- `rg -n '一米|1m' source/_posts/睿抗赛94分复盘.md`：零命中，未确认距离已删除。
- 事实关键词检查：通过，文章同时包含8月18日双版本、8月19日现场测试与没有插电、8月20日正式比赛无异常、`/wit/imu` 与 `imu0_relative=false` 的闭环叙述。
- 首页样式优化后再次运行 `npm run build`：通过，生成的 `public/css/custom.css` 包含弹性布局、静态元信息定位和 `200px` 最小高度规则。
- 本地浏览器视觉验收：未执行；Hexo 预览已在 `4001` 端口启动，但当前没有可用的受控浏览器连接，仍需人工确认桌面端和窄屏实际效果。
- 线上部署检查：博客首页返回 HTTP 200；绕过缓存读取的线上 `css/custom.css` 已包含本次弹性布局、静态元信息定位和最小高度规则。
- 封面检查：`identify` 确认 `ruikang-competition.webp` 为 `1200×675`、约 `73 KB`；`npm run build` 通过，首页、文章页、Open Graph 与 Twitter 图片均引用新封面。
- 封面线上检查：新封面返回 HTTP 200、`content-type: image/webp`，线上睿抗文章页面已引用 `/img/covers/ruikang-competition.webp`。

## Git 提交说明

- 文章修正提交：`699f19c3234fe55ed473bef2836c420045276960`（`docs: correct ruikang competition timeline`）。
- Hexo 发布提交：`3dc7f0f7bb892ceaa1a725a8252175c9eea4413e`（远端 `main`）。
- 首页样式优化提交：`b66fb697628a4ed2acc598568de2a25c785901b8`（`fix: prevent long post title overlap`）。
- 首页样式发布提交：`f69bd6690ab3d8f3fb7c6366b5cdd1af112df06d`（远端 `main`）。
- 睿抗封面提交：`76e62c979a9b547d19ede83219bc6ffa723ff0ae`（`feat: add ruikang competition cover`）。
- 睿抗封面发布提交：`319391203d3bec3740c628ef8cc33ff935e0e375`（远端 `main`）。

## 当前状态、未完成项与风险

- 文章修正：已完成并通过本地构建。
- 发布／部署：已完成；源码已推送到远端 `master`，Hexo 已部署到远端 `main`，线上文章返回 HTTP 200 且包含本次时间线修正。
- 首页样式优化：已推送并部署，线上 CSS 已确认生效；受控浏览器不可用，待人工视觉确认实际排版。
- 睿抗封面：已推送并部署，线上封面文件与文章引用均已确认生效。
- 待验证事项：点4到点5的精确距离仍未知；旧点9问题的单一根因仍无法从现有证据分离。
- 长期记录风险：睿抗工程仓库的当日工作记录没有写入“工程师确认没有插电”和“8月20日正式比赛无异常”这两条最终事实，后续如再从旧记录生成时间线，仍可能重复此次错误。

## 回退方式

- 提交前：可从 Git 基线 `eceba3c17f9ef79b61606dbc8530d4055bba24a6` 取回原文，但未经负责人明确要求不执行覆盖或重置。
- 提交后：使用针对本次任务提交的 `git revert <commit>` 建立可追溯回退，不改写既有历史。
- 首页样式可通过回退本次 `source/css/custom.css` 与工作记录提交恢复，不需要改动文章内容。
- 封面可通过回退本次提交恢复为 `/img/covers/ai-robot-tuning.webp`；旧封面文件未删除。

---

## 博客工程档案功能

### 日期与目标

- 日期：2026-08-23
- 目标：让首页自动跟随最新日志；新增项目档案、文章复现信息卡和资源下载中心；移除已经失效的“学习笔记”入口并发布上线。

### 开始基线

- 分支：`master`
- 开始 HEAD：`5105cdb84f32e28d416be4575041a9d53d3acc69`
- 开始工作区：干净

### 完成内容

- 首页“查看最新记录”和当前运行面板改为读取按日期排序后的第一篇文章，不再在 `home.yml` 中维护一份容易过期的文章副本。
- 首页当前记录可从文章 front matter 读取 `log_system` 和 `log_focus`；未填写时会从标签和分类保守回退。
- 新增 `/projects/` 项目档案页，当前按文章的 `project_id` 聚合睿抗智能侦察车、智慧药房双车系统、YOLOv5 视觉训练和 ISCC 微型工厂，共 4 个项目、5 篇工程记录。
- 为 5 篇工程文章补充 `repro` 元数据，并在文章开头展示运行环境、验证对象、最后验证日期、证据边界和相关材料；没有确认日期的文章不猜测日期。
- 新增 `/downloads/` 资源中心，从文章 `repro.artifacts` 自动汇总 4 个本地训练文件和 `roscar-work-loop` GitHub 项目入口。
- 导航“文章”下新增“项目”和“资源”；首页原“学习笔记”入口改为“项目档案”，并删除旧 `AI学习` 到已失效学习笔记分类的重定向。
- 新页面继续使用现有实验日志视觉系统，状态、环境、结果和构建产物均使用 `LOG / PROJECT DOSSIER / REPRODUCTION NOTE / BUILD ARTIFACTS` 语义呈现。

### 涉及文件

- `_config.anzhiyu.yml`：增加项目和资源导航。
- `scripts/lab-site.js`：增加最新文章派生、复现卡、项目档案和资源中心渲染。
- `source/_data/home.yml`：删除手工维护的当前文章，移除学习笔记入口。
- `source/_data/projects.yml`：维护 4 个项目的名称、状态、技术栈、结果和封面。
- `source/projects/index.md`：项目档案页面入口。
- `source/downloads/index.md`：资源下载页面入口。
- `source/_posts/*.md`：为 5 篇工程文章补充项目归属与已确认的复现信息。
- `source/css/custom.css`：增加复现卡、项目档案和下载中心的桌面端、移动端样式。

### 关键决策与数据边界

- 首页只以 Hexo 当前排序出的最新文章为准，避免 `home.yml` 与文章列表产生两个事实来源。
- 项目说明集中放在 `projects.yml`，文章归属和复现材料留在文章 front matter；资源中心直接汇总文章材料，不再复制下载清单。
- 只有睿抗文章使用 2026-08-20 作为最后验证日期，因为正文明确记录了正式比赛全程；其他文章若缺少同等级证据，则省略日期。
- “学习笔记”只删除首页入口和旧重定向，不删除仍然有效的工程实践、成长记录、分类总览或现有文章。
- `sitemap.xml` 用于向搜索引擎列出页面，RSS/Atom 用于订阅更新，`robots.txt` 用于声明抓取规则和 sitemap 地址；本次按用户选择不实现。

### 验证命令与结果

- 修改前 `npm run build`：通过，基线可正常生成。
- `node --check scripts/lab-site.js`：通过。
- 修改后 `npm run build`：通过，生成 `/projects/`、`/downloads/` 以及带复现卡的文章页面。
- 生成页面断言：通过；首页包含 `LOG 008` 和睿抗文章链接，项目页包含 4 个项目和 5 篇记录，下载页包含 5 项材料，睿抗复现卡包含 Noetic、2026-08-20 和 94 分证据。
- 下载文件检查：通过；`train.py`、`val.py`、`mydata.yaml`、`mydata_yolov5s.yaml` 均存在于生成目录。
- `rg -n "学习笔记" source scripts _config.anzhiyu.yml`：零命中。
- `git diff --check`：通过。
- CSS 独立解析检查：未执行成功，项目未安装 `postcss`；Hexo 完整构建已通过作为替代静态检查。
- 本地浏览器视觉验收：未完成；用户要求直接推送上线查看，页面视觉效果待线上人工确认。

### Git 提交说明

- 功能提交：`9bbab23aa4333ec98ed765c2b3dbe0a82e157eba`（`feat: add project and resource hubs`）。
- Hexo 发布提交：`be312b2c15d00f26010e3cdae9f2b5fbc914566f`（远端 `main`）。

### 当前状态、风险与回退

- 代码与生成页面检查：已完成。
- 源码已推送到远端 `master`；Hexo 已部署到远端 `main`，GitHub Pages 构建状态为 `built` 且没有错误。
- 线上检查：`/`、`/projects/`、`/downloads/` 和睿抗复盘文章均返回 HTTP 200；原 `/categories/学习笔记/` 保持 404，不再有站内入口指向它。
- 待验证事项：桌面端和移动端的实际视觉效果，尤其是项目卡片长标题与窄屏下载卡片。
- 回退方式：使用 `git revert` 回退本次功能提交；首页会恢复手工配置，项目和资源入口及复现卡会一并撤销，不需要改写既有 Git 历史。

---

## Giscus 文章评论

### 日期与目标

- 日期：2026-08-23
- 目标：为博客文章接入基于 GitHub Discussions 的 Giscus 评论，并保持关于页、项目页、资源页等静态页面不显示评论区。

### 开始基线

- 分支：`master`
- 开始 HEAD：`9ebdf4b4f0c22450823d44490916e2933d83080c`
- 开始工作区：干净

### 完成内容

- 为公开仓库 `jiangnanqing188/jiangnanqing188.github.io` 启用 GitHub Discussions。
- 安装 Giscus GitHub App，并将仓库访问范围限制为 `jiangnanqing188/jiangnanqing188.github.io` 这 1 个仓库。
- 将 AnZhiYu 主题评论系统切换为 Giscus，使用 `Announcements` 分类承载文章讨论。
- 使用 `pathname` 将每个文章 URL 映射为独立讨论，并启用严格匹配和懒加载；不同文章不会共用同一评论区。
- 关闭主题原有的匿名邮箱提示，并隐藏 Giscus 不需要的匿名邮箱辅助区域。
- 保留各静态页面 front matter 中的 `comments: false`，评论区只进入文章页面。

### 涉及文件

- `_config.anzhiyu.yml`：启用 Giscus，写入仓库、分类和文章映射参数，关闭匿名邮箱提示。
- `source/css/custom.css`：隐藏 Giscus 不使用的匿名邮箱辅助区域。
- `工作记录/20260823-工作记录.md`：记录外部授权、配置、验证和回退方式。

### 关键决策与权限边界

- 每篇文章按完整路径独立映射讨论；修改文章正文或更新日期不会创建新讨论，只有文章永久链接发生变化才会改变映射。
- 使用 `Announcements` 分类是为了让 Giscus App 代替普通访客创建讨论；评论者仍需登录 GitHub。
- Giscus App 当前只访问博客仓库，权限页面显示其读取元数据并读写 Discussions；没有授权所有仓库。
- GitHub 设置页底部的红色“危险区域”只是暂停和卸载入口，不是安装警告或故障。

### 验证命令与结果

- Giscus 官方分类接口：通过，返回仓库 ID `R_kgDOPTMnKQ` 和 `Announcements` 等 6 个 Discussions 分类，证明 App 已安装且能访问目标仓库。
- GitHub 应用设置页：确认“仅选择存储库”，已选择 `jiangnanqing188/jiangnanqing188.github.io` 这 1 个仓库。
- `npm run build`：通过，Hexo 配置校验成功并生成文章页面。
- 生成文章检查：通过，睿抗复盘页面包含 Giscus 仓库、分类、`pathname`、严格匹配和懒加载参数。
- 静态页面检查：通过，关于页、项目页、资源页、分类页、标签页和音乐页未生成 Giscus 评论容器。
- `git diff --check`：通过，无空白错误。

### Git、当前状态与风险

- 本记录纳入评论配置提交时，源码推送和 Hexo 部署尚待执行；最终提交哈希、部署结果和线上验证以本次任务交付为准。
- 本地配置与构建验证：已完成。
- 待上线验证：文章页能否加载 Giscus iframe、静态页面是否继续不显示评论区。
- 外部依赖：评论依赖 GitHub、Discussions 和 Giscus App；评论者需要 GitHub 账号，网络无法访问 GitHub 时评论区可能加载失败。

### 回退方式

- 源码回退：对本次任务提交执行 `git revert <commit>`，恢复主题评论配置和匿名邮箱辅助样式，不改写历史。
- 外部回退：如确认不再使用 Giscus，可在 GitHub 的 Installed GitHub Apps 页面卸载 giscus；这是独立于源码回退的权限操作，执行前需再次确认。
