# 2026-08-25 YOLOv5数据集文章更新工作记录

## 日期与目标

- 日期：2026-08-25
- 目标：更新YOLOv5模型训练文章，补充面向智能车的数据采集和数据清洗经验，保持文章主线仍然是数据集制作与YOLOv5训练。

## 开始基线

- 仓库：`/mnt/data/jiangnanqing.github.io`
- 分支：`master`
- 开始HEAD：`a8e247e964d195d20a3472a05524ee16801fb9d9`
- 开始工作区：干净

## 完成内容

- 将原文简略的数据采集段更新为以真实车载摄像头数据为主的采集流程。
- 明确画面流畅时先录视频再抽帧；画面不流畅时，再用手机或主从机方案补充。
- 补充抽帧去重、真实场景覆盖和按视频或采集批次划分训练集与验证集的建议。
- 新增数据清洗段，包含严重模糊图、无效帧、近似重复图和残缺目标的处理规则。
- 明确「只露一半的目标不标注」时，不能让含有该目标的图直接成为漏标训练图，应整张移出或安全裁掉残缺区域。
- 补充标注后的漏标、错类别、边界框、类别分布和`train_batch*.jpg`可视化检查。
- 更新文章描述、关键词、标签和日志焦点，并顺延后续章节编号。

## 涉及文件

- `source/_posts/YOLOv5模型训练.md`：更新数据采集、数据清洗及相关元数据。
- `工作记录/20260825-1-YOLOv5数据集文章更新工作记录.md`：记录本次文章更新、验证和回退方式。

## 关键决策与前后变化

- 数据采集从「小车摄像头尚未实际说明，主要介绍手机拍摄」调整为「车载摄像头优先，手机和主从机仅作补充」。
- 新增标注前清洗与标注后质检两道检查，不把训练参数当作数据问题的第一解决方案。
- 残缺目标的处理不使用一刀切规则：任务不需要识别时可不标，但要避免将其变成漏标；任务需要遮挡识别时则使用统一标注阈值。
- 根据用户补充意见，采集硬件细节只点到为止，没有把文章扩展成录制系统配置教程。

## 参考依据

- Ultralytics YOLOv5 Tips for Best Training Results：部署环境代表性、标注完整性、数据划分和背景图建议。
- Ultralytics Annotation Best Practices：残缺与遮挡目标的统一标注规则。
- Ultralytics Dataset Quality Control：重复图、划分泄漏、漏标、错类别和类别不平衡检查。

## 验证命令与结果

- `npm run build`：通过，Hexo成功生成50个文件。
- 生成页检查：通过，YOLOv5文章页和`search.xml`都包含新增的数据采集与清洗内容。
- 章节编号检查：通过，YOLOv5使用和部署下的流程为（1）至（6）且顺序连续。
- `git diff --check`：通过，无空白错误。
- `npm run deploy`：通过，生成站点提交`b4ee86e`已推送到远端`main`。
- 线上文章检查：返回HTTP 200，正文已包含「画面流畅时」、「目标只露出一半」和「数据质量检查」等新内容。

## Git、当前状态与风险

- 文章任务提交：`661a329 docs: expand YOLOv5 dataset guidance`，已推送到远端`master`。
- 文章修改、本地静态站点构建和线上正文检查：已完成。
- Hexo部署：已完成，远端`main`部署提交为`b4ee86e`。
- 待验证事项：用户按实际阅读感受检查线上文章的篇幅和节奏。
- 风险：「可见多少才标注」仍需根据具体比赛任务制定阈值；文章只给出一致性原则，没有虚构通用固定数值。

## 回退方式

- 对本次任务提交执行`git revert <commit>`，可恢复修改前的文章内容，不改写既有历史。
