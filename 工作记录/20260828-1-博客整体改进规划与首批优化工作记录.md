# 2026-08-28 博客整体改进规划与首批优化工作记录

## 日期与目标

- 日期：2026-08-28。
- 目标：将博客从单纯的技术成长记录进一步整理为面向同学、导师和招聘方的个人技术作品集；明确长期协作规则，完成不依赖新证据的入口优化、项目档案增强和第一轮技术质量修正，并只公开用户已确认的正式邮箱。

## 开始基线

- 仓库：`/mnt/data/jiangnanqing.github.io`。
- 分支：`master`。
- 今日开始 HEAD：`b8f152b79ff338d87ed15f97926de896e5f96556`。
- 首批整站优化开始 HEAD：`758f9cf617ffb9152d151c1636f15954b8474694`。
- 项目档案增强开始 HEAD：`642a15957f49fc874e62f8096258c4772c5c7f9d`。
- 开始工作区：干净。

## 完成内容

### 1. 建立项目级协作与整改计划

- 新建根目录 `AGENTS.md`，明确博客定位、目标读者、个人职责、AI 表述、证据等级、隐私边界、整站改进方向、分阶段计划和发布门禁。
- 记录已确认的个人信息：长江大学、计算机科学与技术、大三、AI 与 ROS 机器人方向，以及多个项目中技术主责、系统集成和进度推进的角色。
- QQ、微信和邮箱原文没有写入项目级规则；正式邮箱是否公开仍需用户确认。
- 记录四项比赛成绩的用户口述版本，但要求写入公开页面前核对证书上的比赛全称、年份、赛道和奖项文字。
- 明确本项目文章不得使用 `khazix-writer`，后续修改应直接学习用户现有文章风格。

### 2. 明确智慧农业与 YOLOv5 的关系

- 确认智慧药房获得国三，智慧农业获得国一。
- 智慧农业整车路径主要采用硬编码，用户认为这部分技术代表性较弱，因此国一可以进入成绩列表，但不把整车路径包装成核心技术项目。
- 确认现有《YOLOv5 模型训练》属于智慧农业项目，重点展示实车数据采集、清洗、标注、训练和部署。
- 项目档案名称改为“智慧农业 · YOLOv5 视觉训练”，关于页入口同步改名。
- YOLOv5 文章增加智慧农业项目背景、标签、验证对象和责任说明；文章仍保留“待验证”，因为目前缺少训练指标和比赛现场原始证据。

### 3. 补充搜索引擎入口

- 安装官方 `hexo-generator-sitemap@3.0.1`。
- 在 `_config.yml` 中启用 `sitemap.xml`，打开 `rel`，不把标签和分类详情页额外加入 sitemap。
- 新增 `source/robots.txt`，允许正常抓取并声明线上 sitemap 地址。
- 完整构建后生成的 sitemap 包含首页、关于页、项目页、资源页和文章页面；`robots.txt` 正常复制到站点根目录。

### 4. 强化作品集入口

- 将“项目”从“文章”下的二级菜单提升为一级导航；桌面导航顺序变为“首页、项目、文章、关于”，手机侧边菜单同步生效。
- 首页主按钮由“查看最新记录”改为“查看项目档案”，固定指向 `/projects/`。
- 首页次按钮改为“阅读最新记录”，继续自动读取 Hexo 当前最新文章的链接。
- 首页右侧当前运行面板仍然自动展示最新文章，没有形成第二份手工维护数据。

### 5. 强化关于页身份信息

- 首屏明确写出“长江大学计算机科学与技术专业大三学生”。
- 方向改为“ROS 机器人 · AI 应用”。
- 角色改为“核心开发 · 系统集成 · 项目推进”。
- 状态从“正在学习”改为“项目开发中”。
- 技术区域标题改为“项目中实际使用的技术”，减少只会罗列技术图标的印象。
- 暂未加入奖项正式名称、邮箱、QQ、微信或求职状态。

### 6. 整理重点文章入口

- 为三篇睿抗文章增加统一系列导读：
  - 《睿抗：从初赛到国一》负责技术成长与完整经历。
  - 《睿抗赛 94 分复盘》负责候选方案、实车证据和最终取舍。
  - 《睿抗赛四套视觉方案》负责视觉模块实现、作用和验证边界。
- 成长文章中对 GPT 5.6 Sol 的吐槽、“没插电”和其他关键失败均未删除。
- AI 使用文章在正文第一段提前说明责任边界：AI 提高检索、修改和测试效率，本人负责定义问题、选择方案、组织验证、判断证据和最终部署决定。
- 将“愚钝如我”收敛为“当时的我”，保留成长语义但避免无必要的自我贬低；文章标题和主体风格本次没有整体重写。

### 7. 强化项目职责、协作边界与文章回链

- 项目页从“简介、技术栈、结果、文章列表”扩展为完整的工程档案结构，增加项目周期、团队背景、本人角色、关键问题、本人职责、协作边界和证据状态。
- 四个项目没有机械套用同一份“技术主责”文案：
  - 睿抗明确本人推进代码主线、43 点导航、Noetic 迁移适配、视觉任务集成、候选取舍和最终回退；同时保留工程师调整 IMU 配置、协助确认硬件问题以及队友参与测试的边界。
  - 智慧药房明确本人主要负责双车软件主线、OCR 与导航联调、命名空间隔离、实车测试节奏和版本基线；不把团队其他工作计入个人成果。
  - 智慧农业只展示本人负责的数据采集、清洗、标注、YOLOv5 训练与部署流程，并再次说明整车硬编码路径不作为核心技术成果。
  - ISCC 明确本人负责通信加密和完成加密后的 HMI 组态，两位队友负责编写与修改 PLC 程序。
- YOLOv5 项目状态由“已完成”调整为“待验证”，与现有文章缺少训练指标和比赛现场原始证据的边界保持一致。
- 每张项目卡增加稳定锚点，7 篇带 `project_id` 的文章自动显示“所属项目 + 本人角色”信息带，并可回到对应项目卡查看完整职责、边界与全部记录。
- 桌面端封面改为贯穿整张项目档案，避免正文增长后留下大块空白；手机端继续使用短封面，职责、边界、结果和证据按单列展示。

### 8. 让首页首屏直接说明个人定位

- 首页主标题下增加身份信息带，新访客无需先进入关于页即可看到：长江大学计算机科学与技术专业大三、ROS 机器人与 AI 应用方向，以及代码主责、系统集成和项目推进角色。
- 身份信息继续由 `source/_data/home.yml` 维护，首页脚本只负责渲染，不在 JavaScript 中复制个人资料。
- 桌面端使用三列紧凑信息带，与右侧当前项目面板同时保留在首屏；手机端改为三行键值结构，避免专业和职责文字被强行压缩。
- 全站副标题由“机器人、AI与编程的成长记录”调整为“长江大学 · ROS机器人与AI应用”，博客名“江南的碎碎念”保持不变。
- 归档页和文章页侧栏的个人说明同步为“长江大学计算机科学与技术专业学生，主要做 ROS 机器人与 AI 应用”，替换缺少学校和方向的泛化描述。

### 9. 调整关于页的代表项目优先级

- 将关于页代表项目中的《AI 使用体验》移出项目卡，文章本身继续保留，不再让 AI 主题占据主要能力入口。
- 睿抗智能侦察车进入代表项目首位和大卡位置，说明从 Gazebo 到 43 点导航、视觉和射击主流程的系统集成经历。
- 智慧药房调整为双车系统项目卡，突出导航、OCR 和实车工作流；智慧农业 YOLOv5 视觉训练继续作为计算机视觉代表项目。
- 代表项目区新增“查看完整项目职责与证据”入口，链接到项目档案页，并说明当前共有 4 个项目和 7 篇相关记录。
- 没有在关于页项目卡直接加入尚未核对正式名称的奖项，只展示能够由现有文章说明的项目范围与技术内容。

### 10. 修复站内断链并补充结构化数据

- 对 58 个生成 HTML 页面执行内部 `href`／`src` 检查，发现文章评论区存在主题无条件生成的 `/privacy` 断链；协议相对的 `//cdn.cbd.int` 属于外部 CDN，不是站内断链。
- 当前仅使用 Giscus，匿名评论、访客邮箱和隐私弹窗均未启用，原“匿名评论／隐私政策”容器也已被自定义样式隐藏；生成 HTML 时直接移除该无效容器，没有为了补链接编造隐私政策页面。
- 删除对应的隐藏 CSS，避免继续保留只为掩盖断链存在的样式。
- 首页新增 `ProfilePage` JSON-LD，公开说明笔名、长江大学、ROS、移动机器人、计算机视觉、AI 应用和系统集成方向，以及现有 GitHub 主页。
- 项目页新增 `CollectionPage` 与 `ItemList` JSON-LD，将 4 个项目映射到各自的稳定锚点。
- 8 篇文章新增 `BlogPosting` JSON-LD，使用文章已有标题、描述、封面、日期、关键词、分类和项目关联；未加入联系方式、未经核对的奖项或求职状态。

### 11. 改善移动端缩放与搜索按钮语义

- 生成页面原有 56 处 `user-scalable=no`，会阻止手机用户按需放大页面；在统一的 HTML 生成过滤器中移除该参数，不直接修改主题依赖。
- 为主题搜索弹层的关闭按钮增加 `aria-label="关闭搜索"`，让只有图标的按钮能够被辅助技术识别。
- 关于页头像仍保留空 `alt`，因为整个头像区域已经标记为 `aria-hidden="true"`，属于刻意不重复朗读的装饰内容。

### 12. 增加跳过导航入口

- 55 个常规正文页面在 `body` 起始位置增加“跳到主要内容”链接，默认位于屏幕外，仅在获得焦点时显示。
- 为对应的 `#blog-container` 增加 `tabindex="-1"`，激活链接后焦点会真正落到主内容，而不只是改变地址栏锚点。
- 第一轮技术质量修正又将 404 页的 `.error-box` 标记为可聚焦主内容区域并补入同一跳转链接，因此当前共有 56 个主题页面具备有效的跳过导航入口；两个历史重定向页保持最小跳转文档。

### 13. 建立能力到项目证据的路由

- 在项目档案首屏与项目卡之间增加“能力路由”，让快速浏览者先按能力找到项目，再核对本人职责、协作边界和现有证据。
- 当前能力索引只使用已有项目可以支持的四类内容：导航与实车调试、系统集成与任务主流程、计算机视觉与模型训练、工程流程与限时交付。
- 采用 `CAP 01—04` 的工程路由表，而不是技能百分比、熟练度进度条或新的通用图标卡，避免用无法验证的自评分替代项目证据。
- 4 条能力路由共指向 9 个项目入口；能力说明单独维护，项目名称和锚点仍从现有项目数据读取，不复制第二份项目标题。
- 项目卡增加 `5.5rem` 的锚点顶部留白，避免从能力路由或文章回链进入项目时被固定导航栏遮挡。

### 14. 公开正式邮箱并完成第一轮技术质量修正

- 根据用户明确授权，将 `jiangnanqingwqw@163.com` 作为公开正式联系方式：关于页首屏与底部联系入口、全站侧栏社交入口均改为邮件；QQ 和微信继续不公开。
- 关于页不再把当前内容较少的 GitHub 主页作为主要行动入口；现有文章和资源页中确有上下文的具体仓库链接继续保留，首页 JSON-LD 中的 GitHub 仅作为身份关联，不承担主要能力证明。
- 将 146344 字节的本地搜索索引从全站预加载改为打开搜索时按需加载，减少未使用搜索时的首屏请求。
- 搜索弹层增加对话框名称、输入框标签、状态播报和结果动态区域；关闭按钮、遮罩或 `Escape` 关闭后，键盘焦点回到当前页面的搜索入口，兼容 Pjax 页面切换。
- 生成期修正主题搜索脚本：匹配时继续使用小写文本，但结果标题和正文片段保留 `Ctrl+V`、`AI` 等原始大小写；查询中的 `+`、`[` 等正则特殊字符按普通字符处理，不再触发脚本异常。修正只作用于已确认的主题脚本路径，并在主题锚点变化时告警，不修改 `node_modules`。
- 8 篇文章顶部封面明确标记为装饰图片，补齐全站图片替代文本检查；关于页三张首屏以下项目图恢复延迟加载。
- 统一为新窗口链接补充 `rel="noopener"`，站内页脚首页链接不再无意义地打开新窗口。
- 404 页背景由外部 GIF 改为本地 40KB WebP，并将提示改为“页面可能已经移动，可使用右上角搜索继续查找”；同时补入主内容语义和跳过导航入口。
- 对标签与分类进行源文件核对：当前 31 个标签偏细，但没有空分类、空标签或大小写重复源数据；`python` 与“比赛”是已有历史重定向，不为降低数量机械改写文章标签。

### 15. 核对奖项并制定逐篇重构计划

- 只读检查 `/home/jnq/桌面/荣誉证书` 中的两张图片和两份 PDF，视觉核对证书版面，并使用 PDF 文本提取交叉确认正式名称、届次、赛道和奖项等级。
- 明确排除 2026 年“优秀共青团员”证书，不把它加入技术博客奖项区。
- 核实三项团队比赛结果：2025 年第 22 届全国大学生信息安全与对抗技术竞赛（智能安全赛-全国总决赛-微型工厂模拟产线任务挑战）三等奖；第二十八届中国机器人及人工智能大赛全国总决赛机器人任务挑战赛（智慧药房）三等奖；同届机器人应用赛（智慧农业）一等奖。
- 睿抗证书尚未出具，当前只保留用户确认的 94 分和国一结果，不推测正式届次、赛道全称和奖项文字。
- 奖项均按项目或团队结果表达，不把团队证书写成个人独立奖项；证书编号和队友姓名不进入公开页面。
- 对外定位更新为：主动学习、持续向上，能在陌生领域和有限时间内推进复杂项目；希望继续深造，也愿意进入重视工程实践和成长空间的高质量团队。
- 完整阅读 `source/_posts/` 中现有 8 篇文章，共 1721 行；没有修改正文。
- 新建逐篇重构计划，将 YOLOv5、ISCC 和 AI 使用感受列为深度重构，睿抗成长文和智慧药房赛后工作流列为中度重构，94 分复盘、四套视觉方案和智慧药房比赛时间线列为轻度重构。
- 计划按事实依赖拆为奖项与入口、结构调整、职责边界、项目证据、逐篇精修五个阶段；项目证据等待后续盘点，职责边界由用户后续补充。

### 16. 校正四个项目的分阶段责任边界

- 智慧药房：初赛阶段与另一支队伍的一名同学合作，对方负责代码，本人负责网络和测试；国赛阶段的代码、网络和测试均由本人独立完成。
- 智慧农业：本人负责前期主代码、硬编码路径和授粉 YOLO 模型；路径和模型完成后，后期主要在摄像头异常时介入。整车硬编码路径仍不作为核心技术成果包装。
- 睿抗：更换系统后的比赛代码与系统适配均由本人独立完成；队友主要负责焊接；工程师调整了 IMU 配置并协助确认现场硬件问题。远程调试段和“没插电”段的行动主语同步校正，没有删除 GPT-5.6 Sol 吐槽。
- ISCC 微型工厂：本人负责通信加密和 HMI 按钮制作，两位队友负责编写与修改 PLC 程序。HMI 赛题要求本身就是完成按钮，没有扩写其他 HMI 工作。
- 将四项事实写入项目级规则、文章重构计划、项目档案数据和直接相关正文；只修正责任主语与阶段边界，没有借机改写文章结构或抹去团队贡献。

### 17. 增加代表成绩入口并确认团队规模

- 新增统一成绩数据，集中维护睿抗 94 分国一、智慧农业全国总决赛一等奖、智慧药房全国总决赛三等奖和 ISCC 全国总决赛三等奖；三项证书已核对，睿抗继续标记“证书待出”。
- 首页在主视觉与内容路由之间增加紧凑成绩档案，关于页在个人介绍后增加完整成绩区；两处共用同一数据来源，并明确这些均为团队参赛结果，个人职责和证据继续链接到项目档案。
- 四个重点项目统一确认为 3 人团队；团队规模不替代阶段职责，智慧药房国赛独立承担代码／网络／测试、睿抗代码独立负责等事实保持不变。
- 对外称呼继续使用“江南”，不增加真实姓名。
- 视觉沿用深色实验档案、编号、细线和单一强调色，没有使用奖杯墙、彩色勋章或通用数字四宫格。

## 涉及文件

- `AGENTS.md`：项目定位、事实、边界和整站整改计划。
- `_config.yml`：sitemap 配置和更明确的站点副标题。
- `_config.anzhiyu.yml`：一级项目导航、侧栏个人定位、公开邮件入口、搜索加载方式和本地 404 配置。
- `package.json`、`package-lock.json`：新增 sitemap 生成依赖。
- `source/robots.txt`：搜索引擎抓取与 sitemap 声明。
- `scripts/lab-site.js`：首页主次按钮与代表成绩、项目档案字段、文章项目上下文、关于页成绩注入，以及生成期链接、图片、搜索和 404 可访问性修正。
- `source/js/lab-accessibility.js`：搜索关闭后的焦点恢复和 Pjax 页面兼容。
- `source/_data/home.yml`：首页身份、方向、角色、主次按钮文案与项目入口。
- `source/_data/achievements.yml`：首页与关于页共用的代表成绩数据。
- `source/_data/capabilities.yml`：能力路由、说明及其关联项目。
- `source/_data/projects.yml`：四个项目的角色、职责、问题、协作边界、结果和证据说明。
- `source/projects/index.md`：项目页搜索描述。
- `source/css/custom.css`：项目责任面板、证据结构、文章项目上下文及桌面／手机布局。
- `source/about/index.md`：个人身份、方向、角色、项目入口和正式邮件联系入口。
- `source/_posts/YOLOv5模型训练.md`：智慧农业背景和文章定位。
- `source/_posts/睿抗：从初赛到国一.md`：系列导读。
- `source/_posts/睿抗赛94分复盘.md`：系列导读。
- `source/_posts/睿抗赛四套视觉方案.md`：系列导读。
- `source/_posts/AI使用感受.md`：AI 责任边界和一处自我贬低措辞。
- `source/_posts/智慧药房机器人比赛参赛复盘.md`：初赛／国赛分工和国赛阶段行动主语。
- `source/_posts/国三之后我把智慧药房比赛做成了一套工作流.md`：国赛独立维护、测试与启动边界。
- `source/_posts/ISCC微型工厂模拟赛道经验分享.md`：通信加密、HMI 按钮和 PLC 分工。
- `规划/博客文章重构计划.md`：八篇文章的定位、问题、重构程度、依赖、实施顺序和统一验收标准。
- `工作记录/20260828-1-博客整体改进规划与首批优化工作记录.md`：本记录。

## 关键决策与参数

- 博客采用“外层专业、内层真实”的结构，不把文章统一改成简历腔。
- 四个重点项目的主要职责与独立完成阶段已经确认；团队规模和边界之外的协作细节仍需分别核对。
- 智慧农业一等奖证书已核实，可以进入代表成绩；整车硬编码路径仍不作为核心技术成果，技术展示继续以 YOLOv5 视觉部分为主。
- 正式邮箱 `jiangnanqingwqw@163.com` 已由用户确认公开；QQ 和微信继续不公开。
- sitemap 参数为 `rel: true`、`tags: false`、`categories: false`，优先提交高价值页面，避免把大量薄标签页加入 sitemap。
- 首页项目档案是主行动入口，最新文章是次行动入口；最新文章内容仍从 Hexo 排序结果自动派生。
- RSS/Atom 本次没有添加，因为当前没有明确订阅需求，不为功能数量机械增加依赖。
- 项目页采用“关键问题 → 我负责 → 协作边界 → 结果与证据 → 相关记录”的阅读顺序，不以奖项和技术图标代替能力说明。
- 项目文章只注入紧凑的所属项目和本人角色信息，不把整张项目摘要复制到每篇长文；具体方案与验证仍以各篇正文为准。
- 项目周期只使用文章中能够确认的时间范围；团队人数无法确认的项目只写团队名称或团队项目，不擅自填写人数。
- 首页先回答身份、方向和项目角色；奖项只使用证书已核实文字，机会表述保持积极但不制造“急于求职”的状态。
- 关于页代表项目按系统能力而不是 AI 工具话题排序；AI 文章保留真实使用经验，但不替代机器人和视觉项目本身。
- 主题生成但实际不可见的隐私链接不通过空壳页面补齐，而是在生成阶段移除；未来如果真的收集访客数据，再单独确认并建立正式隐私说明。
- 结构化数据只复用站点当前公开信息，并用 `JSON.stringify` 生成和转义，不手写易失真的第二份项目或文章数据。
- GitHub 当前不作为主要能力入口；不为了求职包装空仓库，现有具体仓库只在确有上下文的文章与资源页保留。
- 性能和可访问性修正沿用现有暗色工程档案视觉，不新增联系表单、社交图标矩阵或无证据的宣传模块。
- 文章重构不追求八篇同构；统一的是事实质量、责任表达和证据边界，叙事、复盘、技术深挖和教程仍保留不同形态。

## 验证命令与结果

- `npm view hexo-generator-sitemap version`：确认当前版本为 `3.0.1`。
- `npm ls hexo-generator-sitemap --depth=0`：通过，项目加载 `hexo-generator-sitemap@3.0.1`。
- `node --check scripts/lab-site.js`：通过。
- `npm run clean`：通过，删除旧数据库与生成目录。
- `npm run build`：通过；完整重建生成 105 个文件，包含 `sitemap.xml` 和 `robots.txt`。
- 首页 CTA 调整后再次执行 `npm run build`：通过。
- `git diff --check`：通过，无空白错误。
- 生成文件检查：通过；sitemap 包含首页、关于页、项目页、资源页和 8 篇文章，robots 指向 `https://jiangnanqing188.github.io/sitemap.xml`。
- 生成导航检查：通过；桌面与手机菜单均包含一级“项目”入口。
- 生成内容检查：通过；关于页身份和角色、三篇睿抗系列导读、AI 责任边界均进入最终 HTML。
- 本地桌面视觉检查：通过；首页、关于页、睿抗成长文章排版正常。
- 本地手机视觉检查：通过；首页 CTA、关于页身份卡、睿抗系列导读和 AI 文章责任边界均能正常换行，没有横向溢出。
- 本地 Hexo 预览服务在检查后已停止，没有保留后台服务。
- 项目档案增强后再次运行 `node --check scripts/lab-site.js`：通过。
- 项目档案增强后执行 `npm run clean` 与 `npm run build`：通过，完整生成 105 个文件。
- 生成页面检查：4 张项目卡均有唯一锚点，7 篇项目文章均生成项目上下文，没有 `undefined`、`null`、空链接或空项目标识。
- 项目页桌面视觉检查：通过；责任与证据层级清楚，左侧封面贯穿项目卡且无空白断层。
- 项目页手机视觉检查：通过；项目周期、团队、角色、职责、边界、结果和证据依次单列展示，没有横向溢出。
- 睿抗文章桌面与手机视觉检查：通过；项目上下文位于日志信息与复现卡之间，不遮挡标题或正文。
- 项目档案视觉检查使用独立的 `4001` 端口；启动前发现 `4000` 已被其他服务占用，因此未结束或修改来源不明的进程。
- 首页定位调整后再次执行 `npm run clean` 与 `npm run build`：通过，完整生成 105 个文件。
- 首页桌面视觉检查：通过；身份信息、主次按钮和当前项目面板仍同时位于首屏，三列信息没有挤压或截断。
- 首页手机视觉检查：通过；身份、方向和角色按三行展示，当前项目面板与内容路线正常下移，没有横向溢出。
- 归档页生成内容检查：通过；侧栏副标题和个人说明均使用长江大学、ROS 机器人与 AI 应用的新定位。
- 关于页代表项目调整后执行 `npm run clean` 与 `npm run build`：通过，完整生成 105 个文件。
- 关于页生成内容检查：通过；睿抗、智慧药房和智慧农业 YOLOv5 三张卡片及项目档案入口均进入最终 HTML，《AI 使用体验》不再出现在代表项目区。
- 关于页桌面长截图检查：通过；睿抗大卡、两张项目小卡和项目档案入口层级清楚，封面裁切正常。
- 关于页手机长截图检查：通过；三张代表项目依次单列展示，项目档案入口正常换行，没有横向溢出。
- 带 `#about-projects` 的无头浏览器截图被主题锚点脚本滚动到空白区域，因此未作为视觉证据；改用无锚点整页长截图完成检查，实际页面内容正常。
- SEO 与链接调整后执行 `node --check scripts/lab-site.js`、`npm run clean` 和 `npm run build`：均通过，完整生成 105 个文件。
- JSON-LD 解析检查：通过；58 个 HTML 中生成 10 个结构化数据块，包括 8 个 `BlogPosting`、1 个 `ProfilePage` 和 1 个 `CollectionPage`。
- JSON-LD 抽样检查：通过；首页身份、项目页 4 个锚点以及睿抗文章的 URL、封面、日期、关键词和所属项目均与现有页面一致。
- 内部资源检查：通过；排除协议相对的外部 CDN 后，58 个 HTML 的站内链接和资源引用为 0 个断链。
- 生成内容检查：通过；`/privacy` 和 `comment-randomInfo` 均不再出现在最终 HTML，Giscus 评论容器和评论管理脚本仍保留。
- 可访问性调整后再次执行 `node --check scripts/lab-site.js`、`npm run clean`、`npm run build` 和 `git diff --check`：均通过，完整生成 105 个文件。
- 生成页面可访问性检查：58 个 HTML 中不再包含 `user-scalable=no`；56 个主题页面保留可缩放 viewport；56 个搜索关闭按钮全部生成 `aria-label="关闭搜索"`，无重复属性。
- 空替代文本检查：仅关于页装饰头像使用 `alt=""`，其父容器已标记 `aria-hidden="true"`，无需修改。
- 跳过导航调整后再次执行完整构建、JavaScript 语法检查和差异空白检查：均通过，仍生成 105 个文件。
- 生成页面检查：55 个常规正文页均有且仅有一个跳转链接和一个可聚焦主内容目标；第一轮技术质量修正后，404 页也具备有效的主内容语义和跳转入口；首页自定义内容仍正常生成。
- 本地运行时检查：桌面 `1440×900` 与手机 `390×844` 下，焦点入口均在左上角正常显示且不挤动页面；激活后 `location.hash` 为 `#blog-container`，`document.activeElement` 为主内容容器。
- 本地预览和无头浏览器检查结束后均已停止，临时脚本、截图与浏览器配置目录已清理。
- 能力路由调整后执行 `npm run clean`、`npm run build`、`node --check scripts/lab-site.js` 和 `git diff --check`：均通过，完整生成 105 个文件。
- 生成内容检查：项目页生成 4 条能力路由、9 个项目链接和 4 个项目目标，所有能力链接均能找到对应项目锚点，没有缺失目标。
- 项目页桌面视觉检查：通过；能力路由保持单表结构，四类能力、说明和项目入口层级清楚，没有抢过项目档案主体。
- 项目页手机视觉检查：通过；能力、说明和项目入口逐条单列展示，9 个链接均正常换行，没有横向溢出。
- 项目锚点运行时检查：从能力路由进入 YOLOv5 项目后，`location.hash` 正确变为 `#project-yolov5`，目标卡片距视口顶部 `88px`，与 `scroll-margin-top: 88px` 一致。
- 能力路由本地预览、截图和锚点检查结束后均已停止，相关临时文件和浏览器配置已清理。
- 联系入口与技术质量修正后执行 `node --check scripts/lab-site.js`、`node --check source/js/lab-accessibility.js`、`node --check public/js/search/local-search.js`、`npm run clean`、`npm run build` 和 `git diff --check`：均通过，完整生成 105 个文件。
- 生成内容检查：58 个 HTML 中有 53 个正式邮箱链接；侧栏和关于页主要入口不再指向个人 GitHub；8 篇文章封面均标记为装饰内容；图片无缺失 `alt`，新窗口链接无缺失 `rel`，站内页脚首页链接不再新开窗口。
- 搜索运行时检查：桌面 `1440×900` 与手机 `390×844` 下，打开搜索前均未请求 146344 字节的 `search.xml`，打开后才加载；搜索“睿抗”返回 4 篇相关文章，标题保持 `Ctrl+V`、`AI` 原始大小写；输入 `AI+` 返回空状态且无 JavaScript 异常。
- 搜索键盘与语义检查：桌面和手机端输入框均自动获得焦点，对话框、输入框、加载状态和结果区域均有相应语义；关闭后焦点恢复到带“搜索”标签的原入口；经 Pjax 切换到 `/about/` 后重复打开与关闭，焦点行为仍保持一致。
- 关于页桌面与手机检查：邮件按钮可见，页面宽度分别与 `1430px`、`390px` 视口一致，无横向溢出；三张首屏以下项目图保持正常显示并使用延迟加载。
- 404 页桌面与手机检查：本地 WebP 背景、搜索提示、主内容语义和跳过导航入口均生效，页面宽度与视口一致，无横向溢出。
- 内部链接与资源复查：扫描 58 个 HTML 的 6333 个本地 `href`、`src` 和 `data-lazy-src` 引用，断链为 0。
- 证书检查：两份中国机器人及人工智能大赛 PDF 均为单页、无加密文件；文本提取与 PNG 渲染内容一致；ISCC PNG 和“优秀共青团员”JPEG 已完成原图视觉核对。
- 文章审阅：8 篇 Markdown 共 1721 行，已逐篇完整阅读并按文章定位、现有优点、结构问题、重构范围和事实依赖形成计划；正文未改动。
- 分阶段责任校正后执行 `git diff --check`：通过；旧的笼统职责和已确认事实之间的直接冲突已清理。
- 执行 `npm run clean` 和 `npm run build`：通过，完整生成 105 个文件；四项责任边界均进入项目页，相关分工文字进入对应文章 HTML。
- 项目页桌面 `1440px` 与手机 `390px` 长截图检查：通过；四张项目卡中的角色、本人职责、协作边界和结果均能正常换行，没有横向溢出或卡片重叠。
- 文章桌面／手机抽样检查：通过；ISCC 桌面端明确显示“通信加密 · HMI 按钮制作”，智慧药房手机端在正文前部显示初赛／国赛分工，项目上下文和复现卡没有遮挡正文。
- 代表成绩入口完成后执行 `node --check scripts/lab-site.js`、`npm run clean`、`npm run build` 和 `git diff --check`：均通过，完整生成 105 个文件。
- 生成内容检查：首页和关于页各生成 4 条成绩记录；四个项目均显示为 3 人团队；首页 JSON-LD 同步包含 4 项奖项；关于页占位节点已被最终内容替换。
- 首页与关于页桌面 `1440px`、手机 `390px` 截图检查：通过；成绩档案层级清楚，证书状态可区分，长比赛名称正常换行，无横向溢出或内容重叠。
- 执行 `npm run deploy`：成功；完整站点产物已推送到远端 `main`，部署提交为 `01af04b`。

## Git 提交说明

- `ab97fe5ee2b27cad0bc851e6906ed9ac947d57c5`：`docs: 记录博客整体改进约定`。
- `d1dad517d9b105f282671c6e27d6e1cdee01d7c4`：`docs: 明确智慧农业项目展示边界`。
- `758f9cf617ffb9152d151c1636f15954b8474694`：`docs: 关联智慧农业与YOLOv5记录`。
- `c5eddc7`：`feat: 增加站点地图与爬虫入口`。
- `57b7e6d`：`docs: 强化作品集入口与文章导读`。
- `68b4efe`：`feat: 优先展示项目档案入口`。
- `d6864e0`：`feat: 完善项目职责与证据档案`。
- `13c20a4`：`feat: 强化首页个人定位`。
- `74f7ac8`：`docs: 调整关于页代表项目`。
- `c1773a7`：`feat: 增加站点结构化数据`。
- `4549b59`：`fix: 改善移动端基础可访问性`。
- `6b52f09`：`feat: 增加跳过导航入口`。
- `7092e53`：`feat: 增加项目能力路由`。
- `c1f752f`：`fix: 完善联系入口与站点质量`。
- 奖项核对与文章重构计划：随本轮任务提交，提交哈希在最终交付中报告。
- 四个项目分阶段责任校正：随本轮任务提交，提交哈希在最终交付中报告。
- 代表成绩入口与团队规模确认：随本轮任务提交，提交哈希在最终交付中报告。
- `main` 站点部署提交：`01af04b`，由 `hexo deploy` 生成并推送。
- `f61819d`：`feat: 增加代表成绩入口`，已推送到远端 `master`。

## 当前状态、未完成事项与风险

- 项目级整改规划：已完成并提交。
- 首批整站小优化：已完成本地修改、构建和桌面/手机视觉检查。
- 项目档案增强：已完成本地修改、完整构建、静态内容核对和桌面／手机视觉检查。
- 首页与侧栏个人定位：已完成本地修改、完整构建和桌面／手机视觉检查。
- 关于页代表项目调整：已完成本地修改、完整构建和桌面／手机视觉检查。
- SEO 结构化数据与站内链接审计：已完成本地修改、完整构建、JSON 解析和内部资源检查。
- 移动端缩放与搜索按钮语义：已完成本地修改、完整构建和生成页面计数检查。
- 跳过导航入口：已完成本地修改、完整构建、产物计数和桌面／手机运行时焦点检查。
- 能力到项目证据的路由：已完成本地修改、完整构建、锚点核对和桌面／手机视觉检查。
- 正式邮件入口与第一轮技术质量修正：已完成本地修改、完整构建、生成内容检查、搜索运行时检查、内部链接复查和桌面／手机视觉检查。
- 三项比赛奖项原文核对与八篇文章重构计划：已完成。
- 四个项目的主要责任边界：已按用户确认事实写入项目数据和相关正文；边界之外的更细协作信息仍待后续补充。
- 首页与关于页代表成绩入口：已完成本地修改、构建、生成内容核对和桌面／手机视觉检查。
- 四个重点项目团队规模：已确认均为 3 人；更细协作信息仍待后续补充。
- 对外称呼：已确认继续使用“江南”，不公开真实姓名。
- 发布／部署：已执行；远端 `main` 已更新到部署提交 `01af04b`。
- 源码推送：已执行；代表成绩入口及此前本地累计修改已随 `f61819d` 推送到远端 `master`。
- 待确认：睿抗证书正式文字，以及已确认边界之外的协作细节。
- 待补证据：YOLOv5 训练指标、智慧农业现存材料、睿抗截图／日志／架构图，以及其他项目目录中的可公开材料。
- GitHub 当前不是优化重点，且属于站外仓库；本次没有修改，也没有将其作为主要能力出口。
- 项目页已按用户确认信息校正智慧药房、智慧农业、睿抗和 ISCC 的主要职责与 3 人团队规模；边界之外的细节不继续推测。
- 新增 sitemap 依赖会增加少量构建依赖；如果未来移除，需要同时删除配置和 `robots.txt` 中的 sitemap 声明。
- sitemap 的 `lastmod` 由 Hexo 页面日期派生，不应把它解释为每篇文章的实车验证日期。

## 回退方式

- 项目级规则可通过回退 `ab97fe5` 和 `d1dad51` 恢复；不建议直接删除后重新创建。
- 智慧农业与 YOLOv5 关联可通过回退 `758f9cf` 恢复。
- sitemap 与 robots 可通过回退 `c5eddc7` 移除，依赖、配置和文件会一起恢复。
- 导航、关于页、系列导读和 AI 边界可通过回退 `57b7e6d` 恢复。
- 首页项目优先 CTA 可通过回退 `68b4efe` 恢复为“最新记录／全部文章”。
- 项目职责、协作边界和文章回链可通过回退本轮任务提交整体恢复，不需要分别删除数据字段或 CSS。
- 首页身份信息和全站副标题可通过回退对应任务提交恢复。
- 关于页代表项目顺序和项目档案入口可通过回退对应任务提交恢复。
- JSON-LD 和隐藏隐私断链修复可通过回退对应任务提交恢复；回退后 `/privacy` 会重新成为生成页面中的隐藏断链。
- 移动端缩放和搜索关闭按钮语义可通过回退对应任务提交恢复；回退后主题页面会重新禁止用户缩放，搜索关闭按钮也会失去可访问名称。
- 跳过导航入口可通过回退对应任务提交恢复；回退后页面视觉不变，但键盘用户需要重新逐项经过顶部导航才能进入正文。
- 能力路由可通过回退对应任务提交整体恢复；项目卡和文章回链仍会保留，只移除项目页顶部的能力索引和锚点留白。
- 正式邮件入口、搜索性能与语义、链接安全属性、文章封面语义和 404 本地化可通过回退对应任务提交整体恢复；不需要改动主题依赖或生成目录。
- 奖项事实、对外定位和文章重构计划可通过回退对应任务提交恢复；本轮没有修改文章正文，也不涉及公开证书文件。
- 四个项目的分阶段责任边界及相关正文主语可通过回退本轮责任校正提交整体恢复。
- 代表成绩入口、统一成绩数据和团队规模可通过回退本轮成绩入口提交整体恢复。
- 所有回退均应使用 `git revert <commit>` 建立可追踪提交，不改写既有历史。
