# 2026-08-25 睿抗视觉方案文章发布工作记录

## 日期与目标

- 日期：2026-08-25
- 目标：将睿抗四套视觉方案 Hexo 草稿纳入个人博客，按现有站点元数据规范构建、推送源码并发布到 GitHub Pages。
- 睿抗工程阶段：`05-real-robot-validation`，来源为睿抗项目 `AGENTS.md`，置信度高；本次仅发布技术文章，不改变机器人候选、部署状态或阶段。

## 开始基线

- Hexo 仓库：`/mnt/data/jiangnanqing.github.io`
- 分支：`master`
- 开始 HEAD：`9aabb47e5d3de6f30a42437e4e33fa5d0abc341e`
- 开始工作区：干净，`master` 与 `origin/master` 一致。
- 文章来源：睿抗工程仓库提交 `92ad270b66c6c218c8e5d96f68a7151c8b747ba8` 中的 `文档归档/睿抗赛四套视觉方案-Hexo文章草稿.md`。
- 本次路径范围：`source/_posts/睿抗赛四套视觉方案.md` 与本记录；未修改主题、站点配置、脚本、其他文章或机器人代码。

## 完成内容

- 将 4313 字正文放入 `source/_posts/睿抗赛四套视觉方案.md`，保留四个二级标题和一个 `<!-- more -->` 摘要分隔符。
- 正文与睿抗工程仓库草稿逐字一致，只补充博客站点现有的 Hexo 元数据。
- 新文章登记为 `LOG 009`、`project_id: ruikang`、`log_status: pending`，复用 `/img/covers/ruikang-competition.webp`。
- 复现信息明确写明：四套方案已有源码、离线测试、车端编译或部分无运动证据，但未完成新 1 至 43 点路线的多轮重复验收。
- 源码提交 `7ffc6f9c214f05fe0888eba4b4e4b6982925017c` 已推送到远端 `master`。
- Hexo 部署提交 `eed8207aa6ed053df773d32f2669522f3d3cd92b` 已推送到远端 `main`。
- GitHub Pages 构建 `1173826479` 状态为 `built`，线上文章已返回并包含完整四节正文。

## 涉及文件

- `source/_posts/睿抗赛四套视觉方案.md`：可发布的 Hexo 文章权威源。
- `工作记录/20260825-2-睿抗视觉方案文章发布工作记录.md`：记录本次跨仓库发布、验证和回退方式。

## 关键决策

- 目标仓库没有更具体的 `AGENTS.md`，沿用仓库现有文章和工作记录规范。
- 文章归入 `工程实践`，并加入睿抗、智能侦察车、ROS、YOLO、RGB-D、OpenCV 和计算机视觉标签。
- 使用 `log_status: pending`，不把局部候选、静态检查或部分无运动证据写成完整实车验收。
- 复用已有睿抗比赛封面，不新增无来源图片。
- 按仓库现有双分支结构发布：源码进入 `master`，Hexo 生成站点进入 `main`。
- 本次没有发现或改变机器人启动、停止、部署、回退命令，不更新睿抗正式运行手册。

## 验证命令与结果

- Hexo front matter YAML 解析：通过；`log_id=009`、`project_id=ruikang`、封面和复现字段均存在。
- 正文一致性：通过；目标文章正文与原草稿一致，正文 SHA-256 为 `347949db5f5b2affee6f6715eebf22c29d36a6a9f19db00864eb636dc74302dc`。
- 文章文件 SHA-256：`cca486e6a2742fde10cd4afc61ab7ec57ed92ed908b347cee47a44dc23fbbe94`。
- 结构检查：通过；正好四个二级标题、一个 `<!-- more -->`。
- `git diff --check`：通过，无空白错误。
- `npm run build`：通过，Hexo 7.3.0 生成 55 个文件。
- 生成页检查：通过；文章页、首页、项目页和 `search.xml` 均包含新文章，页面显示 `LOG 009` 和 `待验证`。
- `git ls-remote`：远端 `master=7ffc6f9c214f05fe0888eba4b4e4b6982925017c`，`main=eed8207aa6ed053df773d32f2669522f3d3cd92b`。
- GitHub Pages：最新构建状态 `built`，对应提交 `eed8207aa6ed053df773d32f2669522f3d3cd92b`。
- 线上正文：通过；四个标题、`LOG 009`、`待验证` 和结尾均可读取。
- 线上 HTML SHA-256：`803c5d11fa0a64f95339f303e4fd2497a4f8c9290bde0984ac9f6abb78d2513b`，与本地生成页一致。

## 部署结果

- 源码远端：`https://github.com/jiangnanqing188/jiangnanqing188.github.io` 的 `master` 分支。
- 站点远端：同一仓库 `main` 分支。
- 在线文章：`https://jiangnanqing188.github.io/2026/08/25/睿抗赛四套视觉方案/`。
- 发布前目标仓库干净，不需要覆盖或备份同名文章；Git 基线 `9aabb47`、源码提交 `7ffc6f9` 和站点提交 `eed8207` 构成可恢复锚点。

## 证据边界

### 已确认

- 新文章源码已进入远端 `master`，生成站点已进入远端 `main`。
- GitHub Pages 构建完成，线上页面与本地生成 HTML 一致。
- 本次提交和部署没有修改机器人代码、ROS 配置或在线参数，也没有连接或控制车辆。

### 有支持但未完成验收

- 文章对四套视觉方案的技术描述来自睿抗仓库源码、工程记录和用户确认事实；其中完整路线与视觉联动仍没有多轮实车验收。

### 未知或待验证

- 移动端、不同浏览器和真实读者视角下的排版与阅读节奏尚待用户人工浏览确认。

## Git、当前状态与回退

- 文章源码提交：`7ffc6f9 docs: 发布睿抗四套视觉方案复盘`。
- 站点部署提交：`eed8207 Site updated: 2026-08-25 16:52:10`。
- 本记录提交哈希在最终交付中报告，不为回填自身哈希追加提交。
- 回退源码时，在 `master` 对文章提交执行 `git revert 7ffc6f9` 后重新构建和部署；不改写既有历史。
- 回退站点时，从源码回退后的状态重新执行 Hexo 构建与部署，不直接强制推送或改写 `main` 历史。
- 本次未产生 ROS 进程、导航目标、速度指令、容器或远程车辆会话。

## 下一步

- 用户打开线上文章，人工检查桌面端和移动端的标题、封面、四节层次及阅读节奏。
- 若需要改文案或新增配图，在 `master` 修改文章权威源后重新执行构建和部署，继续保留验证边界。
